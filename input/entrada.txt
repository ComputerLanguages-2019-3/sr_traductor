resource Queue
op insert(item : int);
op remove() returns item : int;
body Queue(size : int)
var sfore[O:=size-I ] : int;
var fiont := 0, rear := 0, count := 0;
proc insert(item)
if count<size -> store[rear] := item; rear := (reur+l)%size; count++
fi
end;
proc remove() returns item
if count>0 -> item := store[front]; front := (front+l)%size; count--
fi
end
end Queue